[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/api/status"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
gracefulShutdownTimeout = 30

# Optional: number of replicas. Start with 1, scale up later if needed.
numReplicas = 1
